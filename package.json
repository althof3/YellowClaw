{
  "name": "yellowclaw",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm start -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "migrate": "npm run build -w backend && npm run migrate -w backend",
    "start": "npm run start -w backend",
    "check": "npm run check -w backend && npm run check -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
