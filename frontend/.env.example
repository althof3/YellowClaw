# Base URL backend. Kosongkan untuk pakai proxy (package.json) saat dev / nginx saat docker.
# Isi saat run tanpa docker dan BE ada di host/port lain, contoh:
# REACT_APP_API_BASE_URL=http://localhost:4000
REACT_APP_API_BASE_URL=